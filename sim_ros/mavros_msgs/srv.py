"""Simulated mavros_msgs.srv type markers (the rospy.ServiceProxy shim routes by service NAME, so
these are just identity tags passed to ServiceProxy)."""
class SetMode: pass
class CommandBool: pass
class CommandTOL: pass
class WaypointPush: pass
class WaypointClear: pass
